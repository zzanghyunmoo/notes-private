---
tags:
  - git
  - worktree
---
새 브랜치에서 만들면서 워크트리 생성
```bash
git worktree add \
    -b <새 브랜치 이름> \
    <워크트리 디렉토리 경로> \
    master
```

이미 존재하는 브랜치로 워크트리 생성
```bash
git worktree add \
    <워크트리 디렉토리 경로> \
    <존재하는 브랜치 이름>
```